---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: loki-stack
  namespace: monocle
  annotations:
    flux.weave.works/automated: "true"
spec:
  releaseName: loki-stack
  chart:
    repository: https://grafana.github.io/loki/charts
    name: loki-stack
    version: 0.23.0
  values:
    loki:
      enabled: true
    grafana:
      image:
        repository: grafana/grafana-dev
        tag: master-a187500c0e6fc3d919a3dd71d85a11aae912d163-ubuntu
      enabled: true
      sidecar:
        datasources:
          enabled: true
    prometheus:
      enabled: true
    promtail:
      enabled: true
